# Aztec Aave Wrapper - Environment Configuration
# Auto-generated to match SDK version

# Aztec Docker image version - must match SDK version
AZTEC_VERSION=3.0.0-devnet.6-patch.1

# PXE HTTP port (external)
PXE_PORT=8081

# Enable prover in PXE (default: false for faster local dev)
PXE_PROVER_ENABLED=false

# L1/Target configuration
ANVIL_L1_PORT=8545
ANVIL_L1_CHAIN_ID=31337
ANVIL_TARGET_PORT=8546
ANVIL_TARGET_CHAIN_ID=31338
ANVIL_TARGET_BLOCK_TIME=1

# Debug logging filter
AZTEC_DEBUG=aztec:*
