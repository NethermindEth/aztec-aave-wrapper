{
  "name": "@aztec-aave-wrapper/l1",
  "version": "0.0.1",
  "description": "L1 Portal contract for Aztec Aave Wrapper",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "coverage": "forge coverage",
    "fmt": "prettier --write 'contracts/**/*.sol'",
    "fmt:check": "prettier --check 'contracts/**/*.sol'",
    "lint:sol": "solhint 'contracts/**/*.sol'",
    "check": "bun run lint:sol",
    "clean": "forge clean"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-solidity": "^2.2.1",
    "solhint": "^6.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
