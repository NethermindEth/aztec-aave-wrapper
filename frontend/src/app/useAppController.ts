/**
 * Re-export from controller module for backwards compatibility.
 * @deprecated Import from './controller/useAppController' instead.
 */
export {
  type AppController,
  type BridgeState,
  type BusyState,
  formatAmount,
  useAppController,
} from "./controller/useAppController";
